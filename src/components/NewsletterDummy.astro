---
const { title = "Join the mailing list" } = Astro.props;
---
<section id="subscribe" class="px-6 py-16 max-w-xl mx-auto reveal">
  <h2 class="font-hand text-4xl mb-4">{title}</h2>
  <p class="mb-6 text-sm opacity-80">New music, show dates, and occasional notes. No spam.</p>

  <form class="space-y-4" onsubmit="event.preventDefault(); alert('Form disabled in demo');">
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
      <div>
        <label for="fname" class="block text-sm mb-1">First name (optional)</label>
        <input id="fname" name="firstName" type="text" class="w-full rounded border px-3 py-2" autocomplete="given-name" />
      </div>
      <div class="sm:col-span-2">
        <label for="email" class="block text-sm mb-1">Email</label>
        <input id="email" name="email" type="email" required class="w-full rounded border px-3 py-2" autocomplete="email" />
      </div>
    </div>

    <button type="submit" class="btn bg-midgreen text-white font-medium px-4 py-2 rounded hover:bg-forest">
      Subscribe
    </button>

    <p class="text-sm mt-3 opacity-60">This demo form is not connected.</p>
  </form>
</section>
